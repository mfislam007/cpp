#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	double pi = 3.142;
	double Area;
	int radius;
	cout << "Input circle radius:";
	cin >> radius;
	Area = pi * pow(radius,2);
	cout << "Circle area with the given radius: " << Area << "\n";
}