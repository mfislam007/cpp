class AskAndPrint{
public:
    void DoYourThing(char str[]);
};

void AskAndPrint::DoYourThing(char str[])
{
    cout << str;
    cin.get(str, 20);
    cout << str << endl;
}
int main()
{
char charstring[] = "Write the text for printing:;
AskAndPrint thing;
thing.DoYourThing(Charsring);
}